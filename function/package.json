{
  "name": "function",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx functions-framework --target=ingestVideos --source=dist/",
    "build": "tsc",
    "gcp-build": "",
    "dev": "nodemon --watch src -e ts --exec 'npm run build && npm run start'"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^5.0.5",
    "axios": "^1.18.1",
    "nodemon": "^3.1.14",
    "pg": "^8.22.0",
    "uuid": "^14.0.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/pg": "^8.20.0",
    "typescript": "^7.0.2"
  }
}
